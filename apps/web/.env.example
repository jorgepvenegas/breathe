# No build-time API URL needed — the frontend uses relative paths.
# Caddy proxies /api/*, /patterns/*, /sessions/* to the backend in production.
# Vite dev proxy is configured in vite.config.ts for local development.
